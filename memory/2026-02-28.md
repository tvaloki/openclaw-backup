- Missing tessie, smart-route, daily-news-digest commands; need to install/restore them for brief generation.
- Successfully handled reminder delivery via direct concise reply.
- Prioritized restoration of commands as pending follow-up.
- Recent plan review (2026-03-06): adopted declarative connection‑profile store and Python pre‑flight validator for Supabase PSQL; identified critical flaws (URI parsing, port whitelist, service‑string mapping, IPv4 reachability, SSL fingerprint, dry‑run limits), missing assumptions (static network, single‑process, immutable CSV, always‑available env vars, PostgreSQL version compatibility, observability pipeline readiness, lock‑contention tolerance, backup sufficiency), better alternative: fully declarative migration pipeline with Python validator, SAVEPOINT transaction, rolling update, IPv6 fallback tunnel; acceptance conditions: profile file exists, validator executable returns 0, dry‑run succeeds, IPv4 resolution succeeds, SSL fingerprint validation passes, metrics emitted and consumable; outstanding follow‑ups: implement validator, write/test validator, verify dry‑run passes, integrate into CI pipeline.