
## Gemini/OpenClaw Fix Log (for Doug) — 2026-03-05
- Investigated repeated Gemini 3.1 failures after model switches.
- Confirmed failures progressed from API_KEY_INVALID/API key expired to quota/rate-limit once auth mismatch was fixed.
- Found root mismatch: OpenClaw used google:default key from `~/.openclaw/agents/main/agent/auth-profiles.json`, which differed from `/etc/openclaw/openclaw.env`.
- Synced Google key in auth-profiles with `/etc/openclaw/openclaw.env` key and restarted gateway process cleanly.
- Verified listener and gateway reachability recovered (`openclaw status` reachable/auth OK).
- Post-fix behavior: Gemini 3.1 now returns `RESOURCE_EXHAUSTED` / API rate limit (no longer invalid key).
- Probe details show free-tier limits effectively 0 for gemini-3.1-pro on this project (`GenerateRequests*` and `GenerateContentInputTokens*` violations).
- Codex path remains healthy (OAuth valid, successful turns).

## Session Rollup — 2026-03-05
- Gateway reliability work: repeatedly investigated/authored fixes for gateway token mismatch (`gateway.auth.token` vs `gateway.remote.token`), manual restart path (non-systemd), and health probe verification.
- Model operations: verified Codex healthy; probed Gemini and confirmed quota/billing/project gating issues initially; later user corrected Google project + billing and Gemini became available in-session.
- Built and published skills:
  - `dual-agent-debate` v0.1.0 → v0.3.0 (hardening, OAuth fallback, safer storage, schema-aware inserts).
  - `dual-agent-solver` v0.1.0 (two-agent solution workflow with OpenClaw as primary solver, memory persistence).
- Added routing preference to long-term memory: FREE-first workflow with Codex escalation for high-stakes tasks.
- Email capability setup: installed/build `gog`, completed OAuth flow troubleshooting (test users, credentials, Gmail API enablement), and successfully sent technical runbook emails to work address.
- OpenBrain import support: retrieved prior memory context for ChatGPT summary import into `public.thoughts`, produced adjusted SQL plan, emphasized running full script locally due to temp-table session scope and `\copy` constraints.
- Connection troubleshooting: captured/normalized Supabase PostgreSQL URI variants discussed (pooler/direct patterns), guided TLS and host/user/port validation steps.
- User preference reinforced: explicitly check Doug context before declaring data unavailable.
