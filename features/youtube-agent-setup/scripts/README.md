# Scripts

Planned scripts:

- `fetch_video_meta.py` — pull video/channel metadata via YouTube Data API v3
- `fetch_comments.py` — pull top-level comments + replies with pagination
- `fetch_transcript.py` — transcript retrieval with safe fallback behavior
- `run_pipeline.py` — unified runner for one-shot fetch jobs
